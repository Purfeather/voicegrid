"""Domain-oriented repository boundaries.

The legacy ``desktop.backend.repository`` module remains a compatibility facade
for workers and older tests while API and services migrate to these modules.
"""
