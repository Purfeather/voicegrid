# MOSS-VoiceGenerator Quadro RTX 5000 验收报告

- 日期：2026-08-13
- 状态：通过
- 硬件：NVIDIA Quadro RTX 5000 16GB（CUDA Compute Capability 7.5）
- 稳定运行策略：FP32 模型、FP32 采样、Math SDPA
- 模型：MOSS-VoiceGenerator 1.7B + MOSS-Audio-Tokenizer
- 输出：模型原生 24 kHz、单声道、WAV PCM-24
- 系统显存峰值：10282 MiB
- 模型内 CUDA 峰值：约 8176 MiB
- 释放模型后显存：约 1.59 GiB（系统基础占用）

## 固定样本

| 样本 | 目标 | 实际时长 | 首次加载/总耗时 | 结果 |
| --- | --- | ---: | ---: | --- |
| A | 疲惫沙哑的老年男性 | 4.88 秒 | 约 23.20 秒 | 通过 |
| B | 明亮清澈的年轻女性 | 3.84 秒 | 约 9.75 秒 | 通过 |
| C | 温暖沉稳的中年男性纪录片旁白 | 6.16 秒 | 约 10.88 秒 | 通过 |
| D | 热情粗犷的美式酒馆老板 | 4.24 秒 | 约 9.64 秒 | 通过 |

四个样本均通过格式、采样率、声道、有限值、峰值、削波、响度与静音占比检查；没有 NaN、空音频或残缺正式文件。

## 链路验证

- 长任务可安全停止，状态为 `cancelled`，没有残留 `.partial.wav`。
- 工作进程终止后可自动重建，并再次生成 4.88 秒有效音频。
- 样本 C 已保存为共享音色 `验收音色_20260812_纪录片`。
- 共享音色已在语音合成模块中完成 4.64 秒、48 kHz、单声道、PCM-24 短句验证。
- 模型释放后运行状态恢复为空闲，显存回落至系统基础占用。

完整机器可读记录保存在同目录的 `voice-generator-20260813-004927.json`（本机生成文件，不纳入 Git）。
